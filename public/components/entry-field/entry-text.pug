p.text-serif(
    ng-class="{ small: fieldKey === 'notes' }"
    ng-show="!isEditing()",
    ng-click="startEditing()",
) {{ entry[fieldKey] }}

textarea.focus.form-control.text-serif(
    ng-class="{ small: fieldKey === 'notes' }"
    rows="10",
    ng-show="isEditing()",
    ng-model="entry[fieldKey]",
    ng-change="edited(fieldKey)",
)
